{% extends "baseadmin.html.twig" %}

{% block body %}
    <h2>Détail visite :</h2>
    {{ form_start(formvisite) }}
    <div class="row">
        <div class="col">
            <div class="row">
                <div class="col-auto">
                    {{ form_row(formvisite.datecreationstring) }}
                </div>
                <div class="col-auto">
                    {{ form_row(formvisite.note) }}
                </div>
                <div class="col-auto">
                    {{ form_row(formvisite.tempmin) }}
                </div>
                <div class="col-auto">
                    {{ form_row(formvisite.tempmax) }}
                </div>
                {{ form_row(formvisite.ville) }}
                {{ form_row(formvisite.pays) }}
            </div>
            <div class="col">
                
            </div>
        </div>
        <div class="row0">
            <div class="col">
                {{ form_row(formvisite.avis) }}
            </div>
        </div>
    </div>
    {{ form_end(formvisite) }}
{% endblock %}